# PostgreSQL — Supabase or any Postgres host
DATABASE_URL="postgresql://user:password@localhost:5432/dvla_idp?schema=public"
DIRECT_URL="postgresql://user:password@localhost:5432/dvla_idp?schema=public"

# JWT
JWT_SECRET="change-me-to-a-long-random-secret"

# Server
PORT=5000
NODE_ENV=development

# Frontend (for CORS)
FRONTEND_URL=http://localhost:3000

# Email (optional)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=noreply@example.com
SMTP_PASS=password
SMTP_FROM="DVLA IDP System <noreply@example.com>"
