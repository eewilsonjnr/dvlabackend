# PostgreSQL — Supabase or any Postgres host
DATABASE_URL="postgresql://postgres:Year22025@localhost:5432/dvla_idp?schema=public"
DIRECT_URL="postgresql://postgres:Year22025@localhost:5432/dvla_idp?schema=public"

# JWT
JWT_SECRET="change-me-to-a-long-random-secret"

# Audit log encryption (AES-256-GCM) — must be exactly 32 chars in production
AUDIT_ENCRYPTION_KEY="dvla-audit-key-change-in-prod-!!"

# Server
PORT=5000
NODE_ENV=development

# Frontend (for CORS)
FRONTEND_URL=http://localhost:3000

# Email — configure with your Gmail or SMTP credentials
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=andydentgh@gmail.com
SMTP_PASS=ghvzlcvnhteponuo
SMTP_FROM="DVLA Ghana IDP <andydentgh@gmail.com>"

